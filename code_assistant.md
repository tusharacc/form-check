# Project: formcheck

> Auto-generated by code-assistant. Do not edit manually — it will be overwritten after each pipeline run.

## Project

- **Type**: Other (unknown)
- **Language**: Unknown
- **Package manager**: unknown
- **Test runner**: unknown

## Module Layout

- backend/
- renderer/

## Architecture

## Analysis

### Problem Breakdown
- **Real-time Exercise Posture Coach**: Monitor exercise sessions using webcam, identify exercises, overlay skeleton, provide real-time feedback, and log sessions.
- **Tech Stack**: Electron + Python with specific libraries for camera access, pose detection, WebSocket communication, text-to-speech (TTS), sound alerts, and SQLite database.

### Constraints
- Use the specified tech stack without deviation.
- Ensure real-time performance for video feed and analysis.
- Handle edge cases like calibration failures, network timeouts, and process termination.

## Proposed Approach

### Module Layout
1. **Electron Frontend**
   - `main.js`: Main entry point to create BrowserWindow and manage Python backend lifecycle.
   - `preload.js`: Expose IPC API for Electron ↔ Python communication.
   - `renderer/index.html + style.css + app.js`: UI components, WebSocket client, and state management.

*(truncated)*

## Key Files

- main.js
- preload.js
- renderer/index.html
- renderer/style.css
- renderer/app.js
- backend/server.py
- backend/__init__.py
- backend/camera.py
- backend/calibrator.py
- backend/analyzer.py
- backend/voice.py
- backend/sound.py
- backend/journal.py

## Current Status

**? UNKNOWN** — outcome unknown (2026-03-29)

## Acceptance Criteria

| Criterion | Result |
|-----------|--------|
| `npm start` launches app and Python backend starts within 3 seconds. | UNKNOWN |
| Calibration screen shown on launch — user holds still for 5 seconds, progress bar updates. | UNKNOWN |
| After calibration, video feed renders at ≥10fps with selfie segmentation (background dimmed). | UNKNOWN |
| Skeleton overlay drawn only when key joint confidence ≥ 0.5. | UNKNOWN |
| After 5 seconds of exercise, first analysis result (exercise name + reps) appears in sidebar. | UNKNOWN |
| WARNING severity → orange text + voice tip spoken by macOS say + single beep. | UNKNOWN |
| CRITICAL severity → red text + voice tip + double beep. | UNKNOWN |
| Stop button saves session to SQLite, journal tab updates with new row. | UNKNOWN |
| Closing window kills Python child process (no orphan processes). | UNKNOWN |
| All network calls go to localhost:11434 only (Ollama). | UNKNOWN |

## Work History

| Date | Task | Outcome |
|------|------|---------|
| 2026-03-29 | Please implement everything described in the requirement document 'requirements. | UNKNOWN |

## Open Requirements

*(populated after `ca --spec /finalize`)*
